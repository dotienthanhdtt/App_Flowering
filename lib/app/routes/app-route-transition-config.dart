import 'package:flutter/material.dart';
import 'package:get/get.dart';

/// Shared default transition configuration for all app routes
const Transition kDefaultTransition = Transition.rightToLeft;
const Duration kDefaultDuration = Duration(milliseconds: 250);
const Curve kDefaultCurve = Curves.easeInOut;
